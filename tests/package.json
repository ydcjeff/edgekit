{
	"name": "edgekit-test",
	"version": "0.0.0",
	"type": "module",
	"private": true,
	"engines": {
		"node": "~20.9.0"
	},
	"scripts": {
		"dev": "vite",
		"build": "vite build && vite build --ssr",
		"preview": "vite preview",
		"test": "playwright test"
	}
}
